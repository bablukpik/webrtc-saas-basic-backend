node_modules
# Keep environment variables out of version control
.env
.env.local
.env.*.local
dist
.vscode
package-lock.json
dist.tar.gz
frontend

# Keep example file
!.env.example

# Don't ignore audio files in public
!public/*.mp3
